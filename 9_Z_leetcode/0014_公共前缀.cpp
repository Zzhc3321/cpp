#include <iostream>
#include <string>
#include <vector>
using namespace std;

string longestCommonPrefix(vector<string>& strs) {
  if (!strs.size()) {
      return "";
  }

  string res="";
  int c = strs[0][0];
  int j=0;
  for(int i=0;i<strs.size();i++){
    if(c!=strs)
  }
}

int main(){
  string s="test",r="test12";
  string a = r-s;
  cout<<a<<endl;
  return 0;
}
